<h3>Lista De cursos</h3><br>

<%= link_to new_god_curso_path, class: 'btn btn-success' do %>
  <%= fa_icon 'plus', text: "Crear curso" %>
<% end %><br><br>
<table class="table table-bordered table-striped">
  <thead>
    <tr>
      <th>Id</th>
      <th>Grado</th>
      <th>Letra</th>
      <th>Año</th>
      <th>acciones</th>
    </tr>
  </thead>
  <tbody>
    <% @cursos.each do |curso| %>
      <tr>
        <td><%= curso.id %></td>
        <td><%= curso.grado %></td>
        <td><%= curso.letra_codigo %></td>
        <td><%= curso.año %></td>
        <td><%= link_to 'asignar alumnos o profesor', god_curso_path(curso), class:'btn btn-default' %></td>

      </tr>
    <% end %>
  </tbody>
</table>

<!--

<%= simple_form_for(@curso) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :nombre %>
    <%= f.input :año %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>

-->


<!--
configuracion para trabajar con la gema COCOON
-->

<%= simple_form_for @curso do |f| %>
  <%= f.input :nombre %>
  <%= f.input :año %>
  <h3>asignaturas</h3>
  <div id='asignaturas'>
    <%= f.simple_fields_for :asignaturas do |task| %>
      <%= render 'asignatura_fields', :f => asignatura %>
    <% end %>
    <div class='links'>
      <%= link_to_add_association 'add asignatura', f, :asignaturas %>
    </div>
  </div>
  <% f.buttons do %>
    <%= f.submit 'Save' %>
  <% end %>
<% end %>

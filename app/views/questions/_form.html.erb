<%= simple_form_for(@question) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :content, required: true%>
  </div>

  <div class="form-actions">
    <%=f.submit "Crear pregunta", class: "btn btn-primary btn-xl"%>
  </div>

<% end %>

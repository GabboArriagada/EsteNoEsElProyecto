<p style="border-bottom: 1px solid black; background-color: #F0F0F0;">
  <li style="font-: large"><td><%= answer.content %></td> |
    -Hace <%= time_ago_in_words(answer.created_at) %> atrás, por <%= answer.user.name %> <%= answer.user.last_name %>-
  <% if can? :destroy, answer %>
  <td>| <%= link_to "Borrar", question_answer_path(answer.question, answer), method: :delete %></td></li>
</p>
  <% end %>

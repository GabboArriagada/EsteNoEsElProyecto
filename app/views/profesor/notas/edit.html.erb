<h1>Modificar Nota</h1>

<%= simple_form_for([:profesor, @nota]) do |f| %>
  <%= f.error_notification %>
  <div class="form-inputs">
    <%= f.input :user_id, as: :hidden, input_html: {value: @user} %>
    <%= f.input :asignatura_id, as: :hidden, input_html: {value: @asignatura} %>
    <%= f.input :nota, placeholder: 'Ej: 70', input_html: {min: 10, max: 70} %>
    <%= f.input :descripcion %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, 'Cambiar Nota' %>
  </div>
<% end %>

<%= link_to "/profesor/asignaturas/#{@asignatura}/notas?asignatura=#{@asignatura}&user=#{@user}" do %>
  <%= fa_icon 'arrow-circle-left', text: 'Atrás' %>
<% end %>
<br>
<br>
<br>
<br>

<%= simple_form_for([:profesor, @presence]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :asistio %>
    <%= f.input :fecha %>
    <%= f.association :user %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>

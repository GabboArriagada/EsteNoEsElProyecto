<%= simple_form_for(@asignatura) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :nombre %>
    <%= f.input :año %>
    <%= f.association :curso %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
